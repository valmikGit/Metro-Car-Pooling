pipeline {
    agent any

    parameters {
        string(name: 'GIT_COMMIT', defaultValue: '', description: 'Commit SHA passed from master pipeline')
        string(name: 'TRIGGERED_BY', defaultValue: 'manual', description: 'Source of trigger')
    }

    environment {
        DOCKERHUB_CREDS = credentials('dockerhub-creds')
        DOCKER_IMAGE = "${DOCKERHUB_CREDS_USR}/user_microservice:latest"
        EMAIL_IDS_TO_SEND = "vaibhav.bajoriya@iiitb.ac.in"
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/valmikGit/Metro-Car-Pooling.git'
            }
        }

        stage('Build') {
            steps {
                echo "Cleaning Lombok from local Maven repository to force fresh download..."
                sh "rm -rf ~/.m2/repository/org/projectlombok"
                echo "Building user with Maven..."
                sh "mvn -f user/pom.xml clean package -DskipTests"
            }
        }

        stage('Test') {
            steps {
                echo "Running unit tests..."
                sh "mvn -f user/pom.xml test"
            }
        }

        stage('Docker Build') {
            steps {
                echo "Building Docker image..."
                sh "docker build -f user/Dockerfile -t ${DOCKER_IMAGE} ."
            }
        }

        stage('Push Docker Image') {
            steps {
                echo "Pushing to DockerHub..."
                sh "echo ${DOCKERHUB_CREDS_PSW} | docker login -u ${DOCKERHUB_CREDS_USR} --password-stdin"
                sh "docker push ${DOCKER_IMAGE}"
            }
        }

        stage('Deploy to K8s') {
            steps {
                echo "Deploying to Kubernetes (Minikube)..."
                sh "kubectl config use-context minikube || echo 'Using current context'"
                sh "kubectl apply -f k8s/user/"
            }
        }

        stage('Verify Deployment') {
            steps {
                echo "Verifying deployment..."
                sh "kubectl rollout status deployment/user -n metro --timeout=300s"
                echo "User service deployment successful!"
            }
        }
    }

    post {
        success {
            echo "Successfully executed the user pipeline"
            // emailext(
            //     to: "${EMAIL_IDS_TO_SEND}",
            //     subject: "SUCCESS: User Pipeline #${env.BUILD_NUMBER}",
            //     body: """
            //     <b>Build successful!</b><br>
            //     Job: ${env.JOB_NAME}<br>
            //     Build Number: ${env.BUILD_NUMBER}<br>
            //     Triggered By: ${params.TRIGGERED_BY}<br>
            //     URL: <a href="${env.BUILD_URL}">${env.BUILD_URL}</a>
            //     """,
            //     mimeType: 'text/html'
            // )
        }
        failure {
            echo "Failed to execute the user pipeline"
            // emailext(
            //     to: "${EMAIL_IDS_TO_SEND}",
            //     subject: "FAILURE: User Pipeline #${env.BUILD_NUMBER}",
            //     body: """
            //     <b>Build failed!</b><br>
            //     Job: ${env.JOB_NAME}<br>
            //     Build Number: ${env.BUILD_NUMBER}<br>
            //     Triggered By: ${params.TRIGGERED_BY}<br>
            //     URL: <a href="${env.BUILD_URL}">${env.BUILD_URL}</a>
            //     """,
            //     mimeType: 'text/html'
            // )
        }
        cleanup {
            cleanWs()
        }
    }
}